version: "3.9"
services:
  edge-agent:
    image: pilliaredrw/edge-monitor:v1
    container_name: edge-agent
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    # environment:
    #   - AGENT_TOKEN=${AGENT_TOKEN}