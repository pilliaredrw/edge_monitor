FROM python:3.11-slim
WORKDIR /app
# 只复制后端需要的依赖
COPY requirements.txt .
RUN pip install -r requirements.txt
# 复制后端源码
COPY . .
# 暴露 9000 端口
EXPOSE 9000
CMD ["uvicorn", "dash:app", "--host", "0.0.0.0", "--port", "9000"]